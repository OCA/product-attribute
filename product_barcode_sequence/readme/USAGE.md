Automatic Generation:

When you create a new product in a category configured for barcode generation,
the system will automatically assign a unique EAN-13 barcode.

Manual Generation:

For existing products without barcodes:

1. Select the products from the product list
2. Use the **Generate Barcode** action
3. The system will generate barcodes based on their category configuration
