1. Configure the available document types from *Sales \> Configuration \>
   Products \> Document Types*.
2. On any product document, set its *Document Type* field to classify it
   (for example as a Document, Data Sheet, or Safety Data Sheet).
