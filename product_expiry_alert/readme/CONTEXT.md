For the time being, Odoo is computing an alert field for `expiration_date` one that has
its date before as today.

In some warehouses, we need to get the lots that have the other fields 'expired' (date before today):

- Removal date
- Alert date
- Use Date
