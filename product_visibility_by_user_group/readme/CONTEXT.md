Some companies need to expose only part of the product catalog to specific
internal users, for example sales teams working on a limited catalog.

The module adds a group-based whitelist on product templates. An empty whitelist
keeps the standard visibility, while a configured whitelist restricts access to
matching user groups. Product managers keep full product visibility for
administration purposes.
