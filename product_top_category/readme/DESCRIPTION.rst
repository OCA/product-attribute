For all categories, compute their top categories. This can be useful for more readable sale reports.

Note :  Categories must have been configured so that the top category is informative. 
This module is not useful if the top category is always "All".
