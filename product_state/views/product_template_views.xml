<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="view_product_template_search_state" model="ir.ui.view">
        <field name="name">product.template.search.state</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_search_view" />
        <field name="arch" type="xml">
            <field name="name" position="after">
                <field name="product_state_id" />
                <filter
                    string="State"
                    name="groupby_state"
                    domain="[]"
                    context="{'group_by' : 'product_state_id'}"
                />
                <separator />
            </field>
        </field>
    </record>
    <record id="product_template_form_view" model="ir.ui.view">
        <field name="name">product.template.common.form</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_form_view" />
        <field name="arch" type="xml">
            <!--
                Until 18.0, ``product.product_template_form_view`` declared an <header/> tag.
                It doesn't exist anymore in 19.0, so we need to add it on our own.
            -->
            <sheet position="before">
                <header>
                    <field
                        name="product_state_id"
                        widget="statusbar"
                        options="{'clickable': '1'}"
                    />
                </header>
            </sheet>
        </field>
    </record>
    <record id="product_template_view_tree_inherit_product_state_id" model="ir.ui.view">
        <field name="name">product.template.tree (product_state_id)</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_tree_view" />
        <field name="priority">50</field>
        <field name="arch" type="xml">
            <field name="type" position="after">
                <field name="state" />
            </field>
        </field>
    </record>
    <record id="action_open_state_products" model="ir.actions.act_window">
        <field name="name">State Products</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">product.template</field>
        <field name="view_mode">kanban,form,list</field>
        <field name="context">{}</field>
        <field name="domain">[('product_state_id', '=', id)]</field>
    </record>
</odoo>
